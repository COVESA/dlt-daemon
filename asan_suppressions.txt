# ASan suppression file for known false positives and pre-existing issues
# False positive: sigaltstack in ASan with threaded programs
# See https://github.com/google/sanitizers/issues/734
# When the housekeeper thread exits, ASan's PlatformUnpoisonStacks() calls
# sigaltstack which falsely detects a stack-buffer-overflow in another
# thread's stack frame (dlt_user_log_check_user_message).
interceptor_via_fun:__interceptor_sigaltstack
